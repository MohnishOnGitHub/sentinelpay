[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not integration"
markers =
    integration: needs a running Kafka broker; excluded from default pytest
    spark: needs a local SparkSession (Java + PySpark)
